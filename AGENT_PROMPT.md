# Agent Prompt: Finalize and Push Trace Resolve

You are working on a complete GenLayer Portal spinner submission named **Trace Resolve**.

The repository package already contains the intended design and implementation.

## Design is already approved

Do NOT redesign the spinner.

Do NOT replace the approved motion.

Do NOT add orbiting dots, rings, large petals, validator dots, external shards, large glows, or other decorative elements that compete with the GenLayer mark.

The approved design is:

- GenLayer mark is pink/magenta
- the three logo pieces move only a few pixels into alignment
- after alignment, a clearly visible pale pink-white highlight sweeps through the logo
- the highlight is clipped inside the GenLayer mark
- the logo remains the dominant visual element at all times
- default duration is 1.65s
- same production asset works on light, neutral and dark surfaces
- reduced motion resolves to a static logo

## Your job

1. Inspect every file in the repository.
2. Preserve the approved spinner design.
3. Replace all placeholders:
   - `YOUR_GITHUB_USERNAME`
   - `YOUR_REPO_NAME`
4. Run:
   ```bash
   npm test
   ```
5. Fix only genuine implementation, documentation, broken-link, accessibility, deployment or validation issues.
6. Confirm `src/trace-resolve.svg` and `docs/trace-resolve.svg` are byte-identical.
7. Confirm the showcase uses the exact `docs/trace-resolve.svg`.
8. Confirm the loading-state demo uses the exact `docs/trace-resolve.svg`.
9. Confirm sizes 16, 20, 24, 32, 48 and 64 are shown.
10. Confirm light, neutral and dark surfaces are shown.
11. Confirm `prefers-reduced-motion` works.
12. Confirm the visible trace is pale pink/white and visibly crosses the mark.
13. Do not introduce unnecessary dependencies.
14. Commit all final changes.
15. Push to the GitHub repository I give you.
16. Enable or prepare GitHub Pages from `main /docs`.
17. Report:
    - repository URL
    - final commit SHA
    - live showcase URL
    - live loading-state URL
    - test result
    - any changes you made

## Important reference rule

The earlier repository `https://github.com/Ifem1/Genlayer-Spinner` may be used only as a reference for submission completeness.

Do NOT copy its Validator Relay visual style, orbiting validator dots, relay motion, showcase styling, or exact animation.

Trace Resolve must remain visually distinct.
